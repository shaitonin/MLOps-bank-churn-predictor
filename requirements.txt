# Kaggle API client (download datasets)
kaggle>=1.6.0 
# Arrow / Parquet (high-performance CSV reading + Parquet writing)
pyarrow>=14.0.0

# YAML config parsing
PyYAML>=6.0.1

# .env file loading (secrets.env support)
python-dotenv>=1.0.0

# ── EDA Core ───────────────────────────────────────────────────────────────────
# Data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# Statistical testing (ANOVA, Kruskal-Wallis, normality tests, etc.)
scipy>=1.11.0

# Post-hoc tests (Tukey HSD) and advanced stats
statsmodels>=0.14.0

# Machine learning (K-Means, DBSCAN, PCA, StandardScaler, silhouette)
scikit-learn>=1.3.0

# ── EDA Visualization ─────────────────────────────────────────────────────────
# Core plotting
matplotlib>=3.7.0

# Statistical visualization (violin plots, KDE, pairplots, heatmaps)
seaborn>=0.12.0

# ── Great Expectations for quality ─────────────────────────────────────────────────────────
great_expectations>=0.15.0

# ── Modeling  ────────────────────────────────────────────────
mlflow>=2.10.0

# Hyperparameter optimization (Bayesian/TPE, pruning, visualizations)
optuna>=3.5.0

# Optimized gradient boosting
xgboost>=2.0.0

# Histogram-based gradient boosting
lightgbm>=4.2.0

# Oversampling for imbalanced classes (SMOTE)
imbalanced-learn>=0.11.0

# Model explainability (SHAP values)
shap>=0.44.0

# ── Deploy and Inference Service ──────────────────────────────────────────────
# Model serialization
joblib>=1.3.0

# REST inference API
fastapi>=0.100.0
uvicorn>=0.23.0

# Interactive visual interface
streamlit>=1.35.0