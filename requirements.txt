# Kaggle API client (download datasets)
kaggle>=1.6.0 
# Arrow / Parquet (high-performance CSV reading + Parquet writing)
pyarrow>=14.0.0

# YAML config parsing
PyYAML>=6.0.1

# .env file loading (secrets.env support)
python-dotenv>=1.0.0

# ── EDA Core ───────────────────────────────────────────────────────────────────
# Data manipulation and analysis
pandas>=2.0.0
numpy>=1.24.0

# Statistical testing (ANOVA, Kruskal-Wallis, normality tests, etc.)
scipy>=1.11.0

# Post-hoc tests (Tukey HSD) and advanced stats
statsmodels>=0.14.0

# Machine learning (K-Means, DBSCAN, PCA, StandardScaler, silhouette)
scikit-learn>=1.3.0

# ── EDA Visualization ─────────────────────────────────────────────────────────
# Core plotting
matplotlib>=3.7.0

# Statistical visualization (violin plots, KDE, pairplots, heatmaps)
seaborn>=0.12.0

# ── Great Expectations for quality ─────────────────────────────────────────────────────────
great_expectations>=0.15.0

# ── Modeling  ────────────────────────────────────────────────
mlflow>=2.10.0

# Otimização de hiperparâmetros (Bayesian/TPE, pruning, visualizações)
optuna>=3.5.0

# Gradient boosting otimizado
xgboost>=2.0.0

# Gradient boosting baseado em histogramas
lightgbm>=4.2.0

# Oversampling para classes desbalanceadas (SMOTE)
imbalanced-learn>=0.11.0

# Explicabilidade de modelos (SHAP values)
shap>=0.44.0

# ── Deploy e Serviço de Inferência ────────────────────────────────────────────
# Serialização de modelos
joblib>=1.3.0

# API REST de inferência
fastapi>=0.100.0
uvicorn>=0.23.0

# Interface visual interativa
streamlit>=1.35.0